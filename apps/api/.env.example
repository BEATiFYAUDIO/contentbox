# ContentBox API environment

# Database mode
# basic (SQLite, single-user, zero setup) | advanced (Postgres)
DB_MODE=basic

# SQLite (basic)
DATABASE_URL="file:/home/<user>/contentbox-data/contentbox.db"

# PostgreSQL (advanced)
# DATABASE_URL="postgres://contentbox:contentbox_dev_password@127.0.0.1:5432/contentbox"

# Required for auth/JWT
JWT_SECRET="change-me"

# Where ContentBox stores files/manifests
CONTENTBOX_ROOT="/home/<user>/contentbox-data"

# Bind mode: local (127.0.0.1) or public (0.0.0.0)
CONTENTBOX_BIND=local

# Public-only listener for tunnel traffic
# PUBLIC_PORT=4010

# Pin managed cloudflared version (optional)
# CLOUDFLARED_VERSION=2025.2.0

# Optional public origins (uncomment to use)
# CONTENTBOX_PUBLIC_ORIGIN="https://your-host"
# CONTENTBOX_PUBLIC_BUY_ORIGIN="https://buy.your-host"
# CONTENTBOX_PUBLIC_STUDIO_ORIGIN="https://studio.your-host"
# PUBLIC_INVITE_ORIGIN="https://invites.contentbox.link"

# Optional Cloudflare tunnel settings (uncomment to use)
# PUBLIC_TUNNEL_PROVIDER=cloudflare
# CLOUDFLARE_ACCOUNT="your-account-id"
# CLOUDFLARE_TUNNEL_DOMAIN="contentbox.link"
